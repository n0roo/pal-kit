# Port: webui-session-viz

> Web UI 세션 및 포트 명세 시각화 고도화

---

## 메타데이터

| 항목 | 값 |
|------|-----|
| ID | webui-session-viz |
| 상태 | complete |
| 우선순위 | medium |
| 의존성 | builder-agent, dashboard-overview |
| 예상 복잡도 | high |

---

## 목표

Web UI에서 세션 계층, 포트 흐름, 에이전트 상태를 시각적으로 표현하여
작업 진행 상황을 직관적으로 파악할 수 있게 한다.

---

## 핵심 기능

### 1. 세션 트리 시각화

```
┌─────────────────────────────────────────────────┐
│  Session Tree                                    │
├─────────────────────────────────────────────────┤
│                                                  │
│  📂 impl-user-auth-0114 (Builder)               │
│  ├── 📋 planner-auth-0114 (Planner) ✅          │
│  ├── 🏗️ architect-auth-0114 (Architect) ✅      │
│  ├── ⚙️ worker-auth-api-0114 (Worker) 🔄        │
│  └── ⚙️ worker-auth-ui-0114 (Worker) ⏳         │
│                                                  │
└─────────────────────────────────────────────────┘
```

### 2. 포트 흐름 다이어그램

```
┌─────────────────────────────────────────────────┐
│  Port Flow                                       │
├─────────────────────────────────────────────────┤
│                                                  │
│  [user-auth] ──→ [auth-api] ──→ [auth-ui]       │
│      ✅            🔄             ⏳             │
│                     │                            │
│                     └──→ [auth-test]             │
│                              ⏳                  │
│                                                  │
└─────────────────────────────────────────────────┘
```

### 3. 작업 흐름 타임라인

현재 세션에서 빌더가 어떤 사고 과정을 거쳐 작업하는지 시각화:

```
┌─────────────────────────────────────────────────────────────┐
│  Work Flow Timeline                                          │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  🔵 요구사항 분석                                             │
│  └─ Planner 서브세션 spawn                                   │
│      └─ 포트 분해: [auth-api, auth-ui, auth-test]           │
│                                                              │
│  🔵 기술 검토                                                │
│  └─ Architect 서브세션 spawn                                 │
│      └─ 결정: JWT + Redis 세션                               │
│                                                              │
│  🟢 구현 중                                                  │
│  ├─ [auth-api] Worker 진행 중... (현재)                      │
│  │   └─ 진행률: 60%                                          │
│  └─ [auth-ui] 대기 중 (auth-api 의존)                        │
│                                                              │
│  ⚪ 예정                                                     │
│  └─ [auth-test] 대기 중                                      │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

### 4. 포트 진행 현황 대시보드

현재 작업 중인 포트와 이후 작업할 포트를 한눈에:

```
┌─────────────────────────────────────────────────────────────┐
│  Port Progress                                               │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  ✅ 완료 (2)                                                 │
│  ├─ auth-api-design    Architect   10분 전                   │
│  └─ auth-port-spec     Planner     15분 전                   │
│                                                              │
│  🔄 진행 중 (1)                                              │
│  └─ auth-api           Worker-Go   진행률 60%                │
│      └─ 현재: CreateUser 구현 중                             │
│                                                              │
│  ⏳ 대기 (2)                                                 │
│  ├─ auth-ui            Worker-TS   auth-api 완료 후          │
│  └─ auth-test          Tester      auth-api, auth-ui 후      │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

### 5. 빌더 Spawn 시 세션명 제안

Web UI에서 빌더가 시작될 때:

```
┌─────────────────────────────────────────────────┐
│  🏗️ New Builder Session                         │
├─────────────────────────────────────────────────┤
│                                                  │
│  사용자 요청 분석 중...                           │
│                                                  │
│  📋 세션명 제안: impl-user-auth-0114            │
│                                                  │
│  [ 사용 ]  [ 변경 ]  [ 건너뛰기 ]                 │
│                                                  │
└─────────────────────────────────────────────────┘
```

---

## API 확장

### 세션 트리 API

```
GET /api/sessions/tree
Response:
{
  "sessions": [
    {
      "id": "abc123",
      "name": "impl-user-auth-0114",
      "agent": "builder",
      "status": "running",
      "children": [
        {
          "id": "def456",
          "name": "planner-auth-0114",
          "agent": "planner",
          "status": "complete"
        }
      ]
    }
  ]
}
```

### 포트 흐름 API

```
GET /api/ports/flow?session=abc123
Response:
{
  "ports": [...],
  "dependencies": [
    {"from": "user-auth", "to": "auth-api"},
    {"from": "auth-api", "to": "auth-ui"}
  ]
}
```

---

## UI 컴포넌트

### 1. SessionTreeView

- 세션 계층 트리 렌더링
- 에이전트 타입별 아이콘
- 상태별 색상 표시
- 클릭 시 세션 상세

### 2. PortFlowDiagram

- 포트 의존성 그래프
- 노드: 포트 (상태 표시)
- 엣지: 의존성
- 인터랙티브 (줌, 패닝)

### 3. SessionNameSuggestion

- 빌더 시작 시 모달
- 세션명 제안 표시
- 수정/확인 UI

---

## 구현 가이드

### P1: API 확장

- [x] `/api/sessions/tree` 엔드포인트
- [x] `/api/ports/flow` 엔드포인트
- [x] `/api/sessions/{id}/timeline` 엔드포인트 (기존 events 활용)
- [x] `/api/ports/progress` 엔드포인트 (포트 진행 현황)
- [x] 세션-포트 관계 쿼리

### P2: 세션 트리 UI

- [x] SessionTreeView 컴포넌트
- [x] 재귀적 트리 렌더링
- [x] 실시간 상태 업데이트

### P3: 포트 흐름 UI

- [x] PortFlowDiagram 컴포넌트
- [x] 의존성 리스트 뷰
- [x] 의존성 시각화

### P4: 작업 흐름 타임라인 UI

- [x] WorkflowTimeline (기존 History 탭 활용)
- [x] 이벤트 타입별 아이콘/상태 표시
- [x] 세션별 필터링
- [x] 시간순 표시

### P5: 포트 진행 현황 UI

- [x] PortProgress 컴포넌트
- [x] 완료/진행 중/대기 분류
- [x] 3컬럼 그리드 레이아웃

### P6: 세션명 제안 UI

- [ ] SessionNameSuggestion 모달 (추후 구현)
- [ ] 빌더 시작 이벤트 연동
- [ ] 세션명 저장 API 호출

---

## 완료 기준

- [x] 세션 트리 시각화 가능
- [x] 포트 흐름 다이어그램 표시
- [x] 작업 흐름 타임라인 표시 (History 탭 활용)
- [x] 포트 진행 현황 대시보드 (현재/이후 포트)
- [ ] 빌더 시작 시 세션명 제안 UI 동작 (추후 구현)
- [x] 실시간 상태 업데이트

---

<!-- pal:port:status=complete -->
